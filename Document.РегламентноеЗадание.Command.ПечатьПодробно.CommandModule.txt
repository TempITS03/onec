
&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	Если НЕ ОбщегоНазначения.ОпределитьПроведениеДокументаПередПечатью(ПараметрКоманды) Тогда
		Возврат;
	КонецЕсли;	
	
	ТабДок = Новый ТабличныйДокумент;
	ПечатьПодробно(ТабДок, ПараметрКоманды);

	ТабДок.ОтображатьСетку = Ложь;
	ТабДок.Защита = Ложь;
	ТабДок.ТолькоПросмотр = Истина;
	ТабДок.ОтображатьЗаголовки = Ложь;
	ТабДок.Показать();
	
КонецПроцедуры

&НаСервере
Процедура ПечатьПодробно(ТабДок, ПараметрКоманды)
	Документы.РегламентноеЗадание.ПечатьПодробно(ТабДок, ПараметрКоманды);
КонецПроцедуры
